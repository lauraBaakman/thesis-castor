using UnityEngine;
using System.Collections;

namespace Fragments
{
    public class ICPController : MonoBehaviour
    {
        // Use this for initialization
        void Start()
        {
            Debug.Log("Fragments: ICPController");
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}